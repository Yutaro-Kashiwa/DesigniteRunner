# 準備
0. NAISTサーバーでこのリポジトリをクローン
1. main-command.shに実行したいコマンドを書く
2. projects.txtに分析対象リポジトリを書く


# 実行
1. 0_clean.bashでログを削除（やらなくてもよい）
2. sbatch 2_sbatch_main.sh でバッチ実行
3. 全部終わるまで待つ．squeueで確認
4. 実行が終わったらoutputを見て，すべての処理が完了しているかをみる．ディレクトリ名がtmpとかerrorで終わっていたら失敗している


# 再実行
0. 途中で止まった原因を調べて，解決
1. bash delete_tmp_files.bashを実行して，中途半端に終わったプログラムが生成したファイルを削除
2. 「実行」と同じ手順をする